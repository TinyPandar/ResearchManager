# Research Project Agent Contract

本文件用于约束科研项目中的多 Agent 协作。复制到具体项目后，应补充该项目的领域、数据、硬件和安全约束。

## 角色

- **主 Agent（架构负责人）**：负责与用户讨论、调查现状、提出选项、形成 Draft Plan、做架构决策并汇报结果。主 Agent 是唯一的用户协调入口，不另设 architecture_manager。
- **data_manager**：负责数据生成、标注与坐标合同、质量检查、版本和真实数据预览。
- **implementation_manager**：负责已确认计划中的代码、配置、模型、Pipeline、损失、指标和测试。
- **experiment_manager**：负责正式实验启动、算力选择、监控、检查点、实验平台和结果归档。

稳定复用这些角色。不要以每个临时任务的名称新建 Agent。

## 工作门禁

1. 收到想法后，主 Agent先自然讨论，每轮只问少量关键问题。
2. 必要时进行只读调查并提供选项与建议。
3. 信息足够后，主 Agent给出 Draft Plan。
4. 用户确认计划前，不安排完整编码、数据重生成或正式实验。
5. 确认后按“数据 → 实现 → 实验”顺序交接；不存在对应依赖时可跳过角色。
6. 主 Agent基于可验证证据向用户汇报，不转发未经核对的状态。

## 委派纪律

- 选择能够完成任务的最小角色集合。
- 不打断或改派正在工作的 Agent，除非用户明确改变当前任务。
- 不为预览、检查或一次性脚本额外创建任务型 Agent；交给拥有该产物的稳定角色。
- 并行工作只能用于边界清楚、不会同时修改同一文件或同一外部状态的子任务。
- 子 Agent返回精确路径、命令、版本、测试和剩余风险；主 Agent负责整合。

## 变更安全

- 保留用户已有的未提交修改，忽略无关脏文件。
- 数据集、配置和实验采用新版本路径；未获明确授权不得覆盖历史产物。
- 删除、强制推送、覆盖远端分支或停止实验前，必须明确核对目标和授权。
- 数据预览必须由真实项目流水线生成，不能使用手绘或 AI 图片冒充。

## 记录

具体项目维护三类持久记录：

- `docs/DECISIONS.md`：已接受或废弃的架构、数据和实验决策。
- `docs/EXPERIMENT_LOG.md`：实验命令、配置、数据、运行状态、指标与结论。
- `docs/TASK_HISTORY.md`：实现里程碑、改动文件和验证结果。

记录只描述项目事实和可复现证据，不堆叠 Agent 对话。
