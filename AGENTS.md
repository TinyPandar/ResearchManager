# Research Project Agent Contract

本文件只保留对整个科研项目长期成立的全局规则。具体工作流由 `.agents/skills/` 中的 repo-scoped skills 按需加载；不要把数据、实现、实验的完整 SOP 继续堆进本文件。

## 指令优先级与自主性

- 用户当前的明确要求优先于 skill 中的流程性建议；若两者冲突，按用户要求完成可安全执行的部分，并明确真正的阻塞点。
- 对目标明确、可逆、低风险且位于既定范围内的工作，主 Agent应自行补全常规细节并持续做到完成，不为每一步重复请求确认。
- 只有当缺失信息会实质改变研究结论、接口合同、不可逆外部状态或成本边界时，才提出少量关键问题。
- 只读调查、诊断、小修复、文档更新和已确认范围内的实现，不要求先走完整 Draft Plan。

## 角色与路由

主 Agent 是唯一的用户协调入口，负责理解目标、整合证据、做跨角色决策并最终汇报。稳定复用以下职责，不按临时任务创建新角色：

- `research-planning` skill：研究问题、方法、接口或成功标准尚未稳定时使用。
- `research-data` skill：数据生成、标注、坐标、split、预览和质量验证。
- `research-implementation` skill：模型、代码、配置、Pipeline、loss、metric 和测试。
- `research-experiment` skill：正式实验、GPU 运行、checkpoint、监控和结果归档。

选择能够完成任务的最小 skill/角色集合。任务边界清楚且不会同时修改同一文件或外部状态时，应利用并行委派；否则按依赖顺序交接。角色细节只在对应 skill 触发后按需读取 `agents/` 中的参考文件。

## 需要确认的边界

以下情况在执行前需要用户确认，除非用户已经在当前请求中明确授权：

- 实质改变研究问题、数据语义、标签/坐标合同、核心评价指标或物理实验边界；
- 启动明显昂贵、长时间或会占用共享稀缺资源的正式实验；
- 删除历史数据或结果、覆盖不可变产物、强制推送、重写远端历史；
- 停止或删除正在运行的外部实验、任务或服务。

已经确认的计划范围内，常规代码修改、测试、诊断和可逆修复不需要再次确认。

## 证据与记录

- 区分“观察”“推断”“结论”；不得把未运行、未读取或未验证的状态写成事实。
- 数据、配置和正式实验优先采用新版本或不可变路径，保留能够支持研究结论的历史证据。
- 子 Agent交接应返回精确路径、命令、版本、测试/运行结果和剩余风险；主 Agent负责核对后再对用户汇报。
- 需要持久记录时使用：`docs/DECISIONS.md`、`docs/EXPERIMENT_LOG.md`、`docs/TASK_HISTORY.md`（可从 `templates/` 初始化）。记录项目事实和可复现证据，不堆叠 Agent 对话。

## 完成定义

不要把“完成第一版实现”当作默认停止点。一项工作在适用范围内应持续到：目标产物已生成或修改、必要验证已执行、发现的问题已处理或明确阻塞、可复现信息已记录、限制与剩余风险已说明。

详细生命周期见 `docs/WORKFLOW.md`；skill 的触发边界与步骤以 `.agents/skills/*/SKILL.md` 为准。
